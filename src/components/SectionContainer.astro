---
const { class: className, id } = Astro.props
---

<section
  id={id}
  data-section={id}
  class={`section ${className} scroll-m-20 w-full mx-auto lg:max-w-5xl md:max-w-4xl sm:max-w-2xl`}
>
  <slot />
</section>